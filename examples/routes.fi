// routes.fi
@("web.fi")
@("io.fi")

app = {
	routes : [
		["/greet/<name>", name -> "Hello, " + name],
		["/", _ -> io.read_all(io.open("examples/home.html"))],
		["/code", _ -> io.read_all(io.open("examples/routes.fi"))]
	]
}

web.apply_app(app, 5000)