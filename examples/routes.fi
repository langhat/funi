// routes.fi

app = {
	routes : [
		["/greet/<name>", name -> "Hello, " + name],
		["/", _ -> @read_all(@open("examples/home.html"))],
		["/code", _ -> @read_all(@open("examples/routes.fi"))]
	]
}

@apply_app(app, 5000)